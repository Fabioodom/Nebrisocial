[project]
name = "e2e-tests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "playwright>=1.60.0",
    "pytest-playwright>=0.8.0",
]
